version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 14
        - yarn install --frozen-lockfile
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: /dist/
    files:
      - '**/*'
  cache:
    paths:
      - 'node_modules'
